# ──────────────────────────────────────────────────────────────────────────────
# FreshAgent – AI Adulteration Detection System
# Requirements for Intel Iris Xe / CPU-based Windows systems
# ──────────────────────────────────────────────────────────────────────────────

# Deep Learning (CPU backend; DirectML acceleration loaded separately)
torch==2.2.2
torchvision==0.17.2
torch-directml==0.2.5.dev240914        # Intel/AMD/GPU acceleration via DirectML

# Model & Transfer Learning
timm==1.0.9                            # EfficientNetV2 / ConvNeXt pretrained weights

# Image Processing
Pillow==10.3.0
opencv-python==4.9.0.80
albumentations==1.4.8                  # RandAugment, CutMix helpers

# Numerical & ML utilities
numpy==1.26.4
scikit-learn==1.5.0                    # Stratified split, classification metrics
matplotlib==3.9.0
seaborn==0.13.2

# ONNX export & inference
onnx==1.16.1
onnxruntime==1.18.1                    # CPU-optimised inference

# Explainability
grad-cam==1.5.0                        # pytorch-grad-cam package

# Backend API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9               # For file upload forms
aiofiles==23.2.1
qrcode[pil]==8.2                      # Server-side QR code generation for Phone Camera

# Utilities
tqdm==4.66.4
pyyaml==6.0.1
python-dotenv==1.0.1
